# Local OLED control

The Heltec V4 Companion physical UI includes a Display page. Short PRG presses cycle to that page; a long PRG press disables the OLED persistently. A long PRG press while the OLED is disabled restores it. Bluetooth and LoRa remain active.
